import { createPoliceConfig } from "policia-agent-playwright-config/config";

export default createPoliceConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
